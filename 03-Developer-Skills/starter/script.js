// Remember, we're gonna use strict mode in all scripts now!
"use strict";
/* let arr = [17, 21, 23];
let arr2 = [12, 5, -5, 0, 4];

let printForecast = function (ar) {
  let str = '';
  for (let i = 0; i < ar.length; i++){
      str = str + `${arr[i]} C in ${i + 1} days ... `;
  }
  console.log("..." + str);
};

printForecast(arr); */

// END of first challenge in a new section 
